{
  "name": "weekend",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "npm run holidays && parcel serve index.html",
    "build": "npm run holidays && parcel build index.html",
    "holidays": "ruby ./holidays.rb",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Kieran Huggins",
  "license": "ISC",
  "dependencies": {
    "coffeescript": "^2.6.1",
    "date-fns": "^2.28.0",
    "parcel": "^2.3.2",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "sass": "^1.49.8"
  },
  "devDependencies": {
    "@parcel/transformer-coffeescript": "^2.3.2",
    "@parcel/transformer-sass": "^2.3.2"
  }
}
